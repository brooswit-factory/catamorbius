# catamorbius

Webhook → CloudEvents → SSE event gateway (Jira, GitHub, extensible) with lossless retention and resumable streams.

This is the bootstrap scaffold. The real implementation (CloudEvents envelope, provider adapters, durable log, ingress, README with the full event-format contract) lands in a follow-up PR.

_(scaffold verification: confirms a PR into protected main can go green — KAN-743)_
